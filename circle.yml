---

machine:
  python:
    version: 2.7.10
  services:
    - docker

dependencies:
  pre:
    - pip install docker-py
    - pip install molecule
    - pip install ansible==2.2

test:
  override:
    - molecule test
